# Runtime dependencies (kept intentionally small for the 1 GB Raspberry Pi 3B).
# Use plain uvicorn + websockets (no uvloop/httptools) to avoid native builds on ARM.
fastapi>=0.115
uvicorn>=0.30
websockets>=12.0
aiosqlite>=0.20
httpx>=0.27
feedparser>=6.0.11
pydantic>=2.7
pydantic-settings>=2.3
