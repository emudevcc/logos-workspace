# Bounded intake — source snapshot files

High-signal excerpts captured for provenance (verbatim from the preserved prototype/plan documents).
These live under `files/` so the package auditor can confirm command-written evidence exists for the
linked local folder `/Users/esteban/Documents/code_ai/english_cockpit_os`.

Snapshot files here are small, real evidence extracts — not fabricated repo code.

## /files/ listing (in this package)
- `tokens--english-cockpit-os.html.txt`   : the exact :root CSS token block of the design prototype.
- `accent-map-c2--english-cockpit-implementation.md.txt` : the finalized 14-slot C2 module-accent table.
- `labels--english-cockpit-os.html.txt`   : verbatim module titles used as real UI copy.
